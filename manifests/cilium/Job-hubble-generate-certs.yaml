apiVersion: batch/v1
kind: Job
metadata:
  annotations:
    helm.sh/hook: post-install,post-upgrade
  labels:
    app.kubernetes.io/name: hubble-generate-certs
    app.kubernetes.io/part-of: cilium
    k8s-app: hubble-generate-certs
  name: hubble-generate-certs
  namespace: kube-system
spec:
  template:
    metadata:
      labels:
        k8s-app: hubble-generate-certs
    spec:
      automountServiceAccountToken: true
      containers:
        - args:
            - --cilium-namespace=kube-system
            - --ca-generate
            - --ca-reuse-secret
            - --hubble-server-cert-generate
            - --hubble-server-cert-common-name=*.default.hubble-grpc.cilium.io
            - --hubble-server-cert-validity-duration=94608000s
            - --hubble-relay-client-cert-generate
            - --hubble-relay-client-cert-validity-duration=94608000s
          command:
            - /usr/bin/cilium-certgen
          image: quay.io/cilium/certgen:v0.1.13@sha256:01802e6a153a9473b06ebade7ee5730f8f2c6cc8db8768508161da3cdd778641
          imagePullPolicy: IfNotPresent
          name: certgen
      hostNetwork: true
      restartPolicy: OnFailure
      serviceAccount: hubble-generate-certs
      serviceAccountName: hubble-generate-certs
  ttlSecondsAfterFinished: 1800
